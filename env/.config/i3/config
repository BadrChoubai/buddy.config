# i3 config file (v4)
# See https://i3wm.org/docs/userguide.html for reference

set $mod Mod4

font pango:monospace 12

client.focused          #d45a01 #d45a01 #ffffff  #d45a01 #d45a01
client.unfocused          #222222 #222222 #ffffff  #222222  #222222

# Startup
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

# Drag floating windows with mod + mouse
floating_modifier $mod
tiling_drag modifier titlebar

# Terminal
bindsym $mod+Return exec --no-startup-id alacritty

# Kill focused window
bindsym $mod+Shift+q kill

# App launcher
bindsym $mod+d exec --no-startup-id dmenu_run

# Layout
workspace_layout tabbed
for_window [class=".*"] move container to workspace current
bindsym $mod+f fullscreen toggle
bindsym $mod+a focus parent
bindsym $mod+c focus child

# Focus
bindsym $mod+j focus left
bindsym $mod+semicolon focus right
bindsym $mod+Tab focus next
bindsym $mod+Shift+Tab focus prev

# Workspaces
set $ws0 "0"
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"

bindsym $mod+0 workspace number $ws0
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9

# i3 control
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Status bar
bar {
    status_command i3status
    bindsym button4 nop
    bindsym button5 nop
}

# ------------------------------------------
# CUSTOM KEYS
# ------------------------------------------

# Suspend
bindsym $mod+Shift+period exec systemctl suspend

# Volume
bindsym XF86AudioRaiseVolume exec amixer set Master 5%+
bindsym XF86AudioLowerVolume exec amixer set Master 5%-
bindsym XF86AudioMute exec amixer set Master toggle

# Brightness
bindsym XF86MonBrightnessUp exec brightnessctl set 10%+
bindsym XF86MonBrightnessDown exec brightnessctl set 10%-

# Music
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# switch displays
mode "display" {
        # These bindings trigger as soon as you enter the launch mode
        bindsym $mod+Shift+2 exec xrandr --output DP-4 --primary --auto --output eDP-1 --off; mode default
        bindsym $mod+Shift+3 exec xrandr --output eDP-1 --primary --auto --output DP-4 --off; mode default

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+Shift+d mode "display"

# launch select applications
mode "launch" {
        # These bindings trigger as soon as you enter the launch mode
        bindsym Shift+2 exec --no-startup-id firefox; mode default
        bindsym Shift+3 exec --no-startup-id thunar; mode default
        bindsym Shift+9 exec --no-startup-id chromium; mode default

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+Shift+l mode "launch"

# App assignments
assign [class="Alacritty"] workspace 1
assign [class="Firefox"] workspace 2
assign [class="Spotify"] workspace 0
assign [class="Chromium"] workspace 9
for_window [class="Thunar"] floating enable

# App launchers
bindsym $mod+F5 exec --no-startup-id spotify; workspace 0
